#!/bin/sh


    pkg=$(~/.config/scripts/packages.sh)
    spt=$(~/.config/scripts/spotify.sh)
    mem=$(~/.config/scripts/mem.sh)
    net=$(~/.config/scripts/network.sh)
    date=$(~/.config/scripts/date.sh)
    cpu=$(~/.config/scripts/cpu.sh)
    wttr=$(~/.config/scripts/wttr.sh)
    echo "$pkg | $spt | $cpu | $mem | $net | $wttr |" 
    

